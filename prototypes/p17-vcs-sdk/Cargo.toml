[package]
name = "p17-vcs-sdk"
version = "0.1.0"
edition = "2024"

[dependencies]
mlua = { version = "0.10", features = ["luajit", "serialize"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = []
with-vendored-luajit = ["mlua/vendored"]
