-- Valid plugin manifest (SPEC §9.2). Pure data table, no smith_api (defaults to 1).
return {
  name = "acme/good-plugin",
  version = "0.1.0",
  entry = "init.lua",
  description = "p04 fixture plugin",
  license = "MIT",
}
