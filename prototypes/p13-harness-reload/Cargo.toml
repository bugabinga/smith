[package]
name = "p13-harness-reload"
version = "0.1.0"
edition = "2024"

[dependencies]
mlua = { version = "0.10", features = ["vendored", "serialize", "lua54"] }
ciborium = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
