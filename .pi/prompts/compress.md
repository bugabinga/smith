---
description: Delegate DRY/code-compression review to the project compressor agent
argument-hint: <code area or files to improve>
---

Use the `flow` tool.

Parameters:

- agent: `compressor`
- agentScope: `project`
- confirmProjectAgents: true

Delegate this task verbatim:

$@

Report terse on compressor work.
